import React from "react";

import { HeaderSecond } from "../../UI";

import "./Features.sass";

const Features = () => {
    return (
        <div className="features">
            <HeaderSecond text="Verify safely" />

            <div className="features__container"></div>
        </div>
    );
};

export default Features;
